def removeElement(nums, val):
    k = 0  # Count of elements not equal to val
    for num in nums:
        if num != val:
            nums[k] = num
            k += 1
    return k
nums = [3, 2, 2, 3]
val = 3
k = removeElement(nums, val)
print(k, nums[:k]) 
